{
  "name": "saas",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "concurrently \"bun --cwd backend run dev\" \"bun --cwd web run dev\"",
    "dev:backend": "bun --cwd backend run dev",
    "dev:web": "bun --cwd web run dev"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
